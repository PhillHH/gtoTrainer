# Statusbericht AP01

**Letzter abgeschlossener Task:** AP1.T1.1 — Repo & Projektgerüst
**Datum:** 2026-08-23
**Status:** DONE

---

## Was existiert jetzt

Ein lauffähiges pnpm-Monorepo ohne Fachlogik. `pnpm install`, `pnpm lint`,
`pnpm test` und `pnpm build` laufen fehlerfrei durch.

### Workspaces

| Workspace         | Paket           | Zustand                                                    |
| ----------------- | --------------- | ---------------------------------------------------------- |
| `apps/backend`    | `@gto/backend`  | Fastify 5 + TS, startbar, eine Route `GET /healthz`        |
| `apps/frontend`   | `@gto/frontend` | React 18 + Vite 6, baubar, Platzhalter-Inhalt              |
| `packages/shared` | `@gto/shared`   | `HealthResponse`, `HEALTH_STATUS_OK`, `isHealthResponse()` |

Der Vertragsort ist **real in Benutzung**: Das Backend typisiert `/healthz` mit
`HealthResponse` aus `@gto/shared`, und der Backend-Test prüft die echte
HTTP-Antwort per `isHealthResponse()` gegen denselben Vertrag.

### Toolchain

- Node 20.19.6 (`.nvmrc`), pnpm 9.15.9 (`packageManager`, Corepack)
- TypeScript strikt über `tsconfig.base.json`: `strict`,
  `noUncheckedIndexedAccess`, `noImplicitOverride` — von allen Workspaces geerbt
- ESLint 9 Flat Config monorepo-weit + Prettier 3, konfliktfrei über
  `eslint-config-prettier`
- Vitest 2 in `apps/backend` und `packages/shared` — **3 Tests, alle grün**
- Root-Scripts `dev`/`build`/`lint`/`test` (rekursiv) plus `Makefile` mit
  denselben vier Targets

### Doku-Gerüst

`docs/ARCHITECTURE.md`, `docs/DECISIONS.md` (3 ADRs), `docs/INTERFACES.md`,
`docs/RUNBOOK.md`, `docs/AGENT_GUIDE.md` (6 verbindliche Regeln),
`docs/ap/README.md`, `docs/status/`.

### Datenkonvention

`data/book-source/` angelegt: Inhalt git-ignoriert, `README.md` versioniert.
Dokumentiert die Dateinamenskonvention `pXXXX_YY.jpeg` und die Buch-Markdown-Datei.
Als Pflicht-Input für AP3 zusätzlich in `docs/INTERFACES.md` vermerkt.

---

## Offene Punkte

- **GNU Make ist auf der aktuellen Entwicklungsmaschine nicht installiert.** Das
  `Makefile` ist angelegt und syntaktisch korrekt (Tab-Einrückung), konnte hier
  aber nicht ausgeführt werden. Die `pnpm`-Kommandos sind der verbindliche Weg,
  `make` nur Kurzform.
- **`NODE_ENV=production` in der Shell** lässt `pnpm install` die
  devDependencies überspringen. Im Runbook unter Fehlerbehebung dokumentiert.
- Das Frontend nutzt `@gto/shared` noch nicht — die Abhängigkeit wird erst in
  T1.4 mit dem API-Client aufgenommen.
- Keine Datenbank, kein Auth, kein Layout, kein Deployment, keine CI — alles
  bewusst späteren Tasks vorbehalten.

## Risiken

- Keine. Das Gerüst enthält keine Fachlogik und keine externen Abhängigkeiten
  über die dokumentierten hinaus.

---

## Nächster Task

**AP1.T1.2** — Datenbank (Postgres) und Persistenzschicht.
