# Statusbericht AP04 — Lernstand-Kern · Stand nach T4.2

Stand: 2026-08-28 · Vorgänger: [AP03](./AP03.md) · Kanon: `docs/ap/AP04.md`

Fortschreibender Bericht. T4.1 und T4.2 sind abgeschlossen; T4.3 bis T4.7
stehen aus.

---

## 1. Was nach T4.1 existiert

Das Datenmodell des Lernstands steht. **Struktur, keine Verhaltenslogik** — es
gibt weder eine Schreibstelle noch eine Berechnung; beides ist Sache der
folgenden Tasks.

| Baustein                            | Ort                                                        |
| ----------------------------------- | ---------------------------------------------------------- |
| Schema der sieben Tabellen          | `apps/backend/src/db/schema.ts`                            |
| Migration (Tabellen, Constraints)   | `apps/backend/drizzle/0007_learnstate.sql`                 |
| Append-only-Trigger                 | `apps/backend/drizzle/0008_learning_event_append_only.sql` |
| Geschlossene Mengen, Zeilenverträge | `packages/shared/src/learning.ts`                          |
| Ersteinrichtung (idempotent)        | `apps/backend/src/learning/seed.ts`                        |
| Neuanfang                           | `apps/backend/src/learning/reset.ts`                       |
| Kommando `pnpm learning:reset`      | `apps/backend/src/learning/cli-reset.ts`                   |
| Tests                               | `apps/backend/test/learning/`                              |

## 2. Die Tabellen

| Tabelle                 | Kern                                                                                                                    |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `learning_event`        | Protokoll. Aufrufer-ID, Typ, Quelle, Signalklasse, Konzept, optional Chart, `payload`, Korrekturbezug. **Append-only.** |
| `concept_mastery`       | Score **und getrennt** Konfidenz (je 0–1), Zähler je Signalklasse, letzte Prüfung. Genau eine Zeile je Konzept.         |
| `review_queue`          | SM-2-Zustand: Fälligkeit, Intervall, Ease (1,3–3,0), Wiederholungen, Rückfälle, Ursprung.                               |
| `error_log`             | Zeitpunkt, Kontext, Konzept, Beschreibung, Schweregrad, Muster-Tag (leer bis T4.6), Ereignisbezug.                      |
| `skill_rating`          | Eine Achse je Themenbereich aus T3.2, an die Liste per CHECK gebunden.                                                  |
| `skill_rating_snapshot` | Begleittabelle für den Verlauf ([ADR-0038](../DECISIONS.md)).                                                           |
| `learner_state`         | Genau ein Datensatz: Level, Kapitelposition, Mastery-Schwelle, Mindestanzahl objektiver Anker.                          |

Sieben Tabellen für die geforderten sechs Bereiche: Der Rating-Verlauf liegt in
einer Begleittabelle statt im Rating-Datensatz — die Ablage war ausdrücklich als
begründete Entscheidung gefordert und steht in ADR-0038.

## 3. Zählstände nach Migration und Seed (Datenbank `gto`)

| Tabelle                 | Zeilen |
| ----------------------- | -----: |
| `learner_state`         |      1 |
| `skill_rating`          |     12 |
| `learning_event`        |      0 |
| `concept_mastery`       |      0 |
| `review_queue`          |      0 |
| `error_log`             |      0 |
| `skill_rating_snapshot` |      0 |

Zweiter Seed-Lauf: identisch. Der Seed legt nichts doppelt an und setzt nichts
zurück.

## 4. Was technisch abgesichert ist (nicht nur beschrieben)

Zwölf Invarianten, alle über Constraints oder Trigger — die Liste steht in
[INTERFACES.md 17](../INTERFACES.md). Die drei, die am meisten tragen:

- **Ereignisse sind unveränderlich.** Zwei Trigger weisen UPDATE und DELETE
  bedingungslos ab (SQLSTATE 23001). Ohne das wäre der Replay in T4.2 wertlos
  ([ADR-0039](../DECISIONS.md)).
- **Score und Konfidenz sind getrennt geführt**, dazu ein Zähler je
  Signalklasse. Erst das macht sichtbar, ob ein hoher Score aus objektiven
  Treffern oder aus KI-Bewertungen stammt (Scope-Delta 2).
- **Skill-Ratings hängen an der Liste aus T3.2.** Ein erfundener Themenbereich
  wird abgewiesen, nicht auf einen Default umgebogen.

## 5. Draft-Konzepte (Scope-Delta 3)

Kein Constraint prüft `concept.state`. Mastery, Queue, Fehlerlog und Ereignisse
lassen sich auf `draft`-Konzepten genauso führen wie auf `approved`. Belegt
durch `test/learning/schema.test.ts` → „führt Mastery und Queue auch auf einem
draft-Konzept (Scope-Delta 3)".

Stand des Graphen zum Zeitpunkt dieses Tasks: 168 Konzepte, davon 145 auf
`draft` (die Sichtung durch den Nutzer läuft; der Kanon nennt 161).

## 6. Abgrenzung `learner_state` gegen `config`

Lernbezogenes gehört in `learner_state`, Technisches bleibt in `config`
(INTERFACES.md 17). Faustregel: Würde ein Neuanfang den Wert zurücksetzen?

Folge: Der in AP1 angelegte Schlüssel `learning.mastery_threshold` ist abgelöst.
Der Seed setzt ihn nicht mehr; maßgeblich ist `learner_state.mastery_threshold`.
Auf bestehenden Installationen bleibt die alte Zeile wirkungslos stehen.

## 7. Was nach T4.2 existiert

Der Lernstand hat **eine** Schreibstelle. Ab hier erzeugt kein Modus mehr
selbst abgeleiteten Zustand — er meldet ein Ereignis, und der Rest folgt
daraus.

| Baustein                                     | Ort                                          |
| -------------------------------------------- | -------------------------------------------- |
| Ereignis-Vertrag, Nutzdaten je Typ, Prüfung  | `packages/shared/src/learning.ts`            |
| Ableitungen (reine Funktionen, intern)       | `apps/backend/src/learning/derive.ts`        |
| `recordLearningEvent`, `replayLearningState` | `apps/backend/src/learning/service.ts`       |
| `POST /api/learning/events`                  | `apps/backend/src/learning/routes.ts`        |
| Kommando `pnpm learning:replay`              | `apps/backend/src/learning/cli-replay.ts`    |
| Umgehungsverbot als Lint-Regel               | `eslint.config.js` (`no-restricted-imports`) |

### Der Ablauf

1. **Validierung** — Ereignistyp, Nutzdaten, Konzept, Korrekturbezug. Ablehnung
   feldweise; es wird nichts geschrieben.
2. **Persistenz** — `insert … on conflict (id) do nothing`. Keine Zeile zurück
   heißt: die ID gab es schon, `status: 'duplicate'`, kein Fehler.
3. **Ableitung** — Mastery, Queue, Fehlerlog und Rating werden aus dem Strom
   neu berechnet.
4. **Alles in einer Transaktion.**

### Die Entscheidung, die alles trägt

Die Ableitung rechnet **den ganzen Ereignisstrom des Konzepts neu**, statt ein
Delta zu addieren ([ADR-0040](../DECISIONS.md)). Damit rufen der inkrementelle
Weg und der Replay denselben Code — die vom DoD geforderte Gleichheit ist eine
Eigenschaft der Konstruktion, keine Hoffnung, die ein Test bewacht. Der Preis
ist Rechenaufwand, der bei Single-User-Betrieb nicht ins Gewicht fällt.

Daraus folgen drei Nebenbedingungen, die im Code sichtbar sind: alle
Zeitstempel abgeleiteter Zeilen stammen aus den Ereignissen, `error_log.id`
**ist** die Ereignis-ID, und eine Rating-Achse ohne Ereignisse wird nicht
angefasst.

### Determinismus — als Test, nicht als Zusage

Keine Ableitung benutzt Systemzeit, Zufall oder die Datenbank. Das einzige
`new Date(...)` in `derive.ts` rechnet auf `occurredAt` weiter.
`test/learning/determinism.test.ts` liest das Modul und weist verbotene Aufrufe
nach — die Regel gilt bindend für T4.3 bis T4.5 und fällt dort auf, wenn sie
gebrochen wird.

### Platzhalter, die T4.3 bis T4.6 ersetzen

| Funktion             | Fassung in T4.2                                                      | Ersetzt in |
| -------------------- | -------------------------------------------------------------------- | ---------- |
| `foldConceptMastery` | Score = Mittel der Ergebnisse, Konfidenz = Anteil objektiver Signale | T4.3       |
| `foldReviewQueue`    | Eintrag ab dem ersten Fehlschlag, fällig einen Tag später            | T4.4       |
| `foldSkillRating`    | Mittel der Ergebnisse des Themenbereichs                             | T4.5       |
| `foldErrorLog`       | Eintrag je Ergebnis < 0,5; Schweregrad `high` bei 0, sonst `medium`  | T4.6       |

Die Zähler je Signalklasse, `repetitions`, `lapses` und der Ursprung des
Queue-Eintrags werden schon jetzt vollständig geführt. T4.3 und T4.4 tauschen
den Rumpf der jeweiligen Funktion — die Verdrahtung bleibt unberührt.

### Umgehungsverbot

Verbindlich in INTERFACES.md 18, technisch unterstützt durch eine
ESLint-Regel: Ein Import von `learning/derive.js` außerhalb von
`src/learning/` wird abgewiesen. Nach außen sichtbar sind nur
`recordLearningEvent`, `replayLearningState`, `seedLearningState`,
`resetLearningState` und `countLearningRows`.

## 8. Tests

`pnpm test` über alle Workspaces — **691 Tests grün** (4 übersprungen: die
Live-Smoke-Tests ohne gesetztes `LLM_LIVE_SMOKE`):

| Workspace         | Testdateien     | Tests            |
| ----------------- | --------------- | ---------------- |
| `apps/backend`    | 54 (+1 skipped) | 624 (+4 skipped) |
| `apps/frontend`   | 8               | 46               |
| `packages/shared` | 3               | 21               |

`apps/backend/test/learning/` umfasst jetzt 54 Tests: 30 aus T4.1 (Schema,
Invarianten, Seed) und 24 aus T4.2 (Service, Determinismus, Endpunkt).

Die Suiten aus AP1 bis AP3 sind unverändert enthalten und grün. Ein
bestehender Test wurde in T4.1 angepasst: `test/db/schema.test.ts` erwartet
auch die sieben Lernstand-Tabellen im Tabellen-Listing.

**Kein KI-Aufruf in T4.1 und T4.2** — `llm_call_log` steht unverändert auf
538 Zeilen.

## 9. Was bewusst nicht gebaut wurde

Mastery-Berechnung und Schwellenentscheidung (T4.3), SM-2-Algorithmen (T4.4),
Rating- und Level-Berechnung (T4.5), Muster-Report (T4.6), Read-Endpunkte
(T4.7).

## 10. Offene Punkte und Risiken

- **Die Ereignis-ID hat keinen Default.** Das ist Absicht ([ADR-0037](../DECISIONS.md)):
  Ein Aufrufer, der sie vergisst, soll scheitern statt still ein Duplikat zu
  erzeugen. T4.2 muss sie folglich immer setzen.
- **`TRUNCATE` ist vom Append-only-Trigger nicht abgedeckt** — bewusst, weil der
  Neuanfang genau darauf aufsetzt (ADR-0039). Nebenwirkung: Ein
  `TRUNCATE … CASCADE` auf `concept` oder `book_chapter` reißt den Lernstand
  mit. Das betrifft nur Test- und Entwicklungspfade.
- **Die Kanon-Datei liegt als `docs/ap/AP04 (1).md` im Repo, nicht als
  `docs/ap/AP04.md`,** und ist nicht committet. Inhaltlich stimmt sie mit dem
  Auftrag überein; `docs/ap/` wird vom Agenten nicht angefasst (Regel 6). Zu
  erledigen vom Nutzer.
- Der Rating-Wertebereich ist auf 0–1 festgelegt (CHECK), analog zum
  Mastery-Score. Sollte die EWMA in T4.5 eine andere Skala brauchen, ist das
  eine zusätzliche Migration.

## 11. Nächster Task

**T4.3 — Mastery- & Schwellenlogik:** gewichtete Signale (objektive Treffer >
KI-Bewertung > Selbsteinschätzung), Konfidenz getrennt geführt, Weiterschaltung
mit Mindestanzahl objektiver Anker. Ersetzt `foldConceptMastery`; die
Verdrahtung bleibt unangetastet.
