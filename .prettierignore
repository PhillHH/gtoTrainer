node_modules/
dist/
build/
coverage/
pnpm-lock.yaml
data/book-source/

# docs/ap/ ist Kanon des Nutzers - Regel 6 im AGENT_GUIDE verbietet jede
# Aenderung, auch reine Umformatierung durch Prettier.
docs/ap/
*.tsbuildinfo

# Prompt-Templates und ihre Golden-Dateien (AP2.T2.4): Prettier wuerde Prosa
# umbrechen und damit Golden-Tests unbemerkt rot faerben. Der Inhalt wird
# stattdessen durch die Golden-Tests abgesichert.
apps/backend/prompts/
apps/backend/test/prompts/golden/

# Buch-Fixtures (AP3.T3.1): Prettier normalisiert Markdown-Kursiv von `*` auf
# `_` und wuerde damit genau die Syntax veraendern, die der Parser erkennen
# soll. Die Fixtures bilden die Buchquelle nach und bleiben unangetastet.
apps/backend/test/book/fixtures/

# Import-Reports (git-ignoriert, hier zusaetzlich fuer lokale Laeufe).
data/reports/
